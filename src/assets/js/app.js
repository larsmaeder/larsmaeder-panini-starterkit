// Import vendor .js files; compiled in one app.js in production build
import './vendor/foundation.min';
import './vendor/jquery';
import './vendor/what-input';

$(document).ready(function() {
    // ADD js scripts
});

// Allow important foundation page elements to load
$(document).foundation();
